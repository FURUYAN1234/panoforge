{
  "name": "background",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --base=./",
    "preview": "vite preview",
    "deploy": "npm run release:preflight && gh-pages -d dist",
    "release:preflight": "node scripts/release_preflight.mjs"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@google/genai": "^2.2.0",
    "three": "^0.184.0"
  }
}
