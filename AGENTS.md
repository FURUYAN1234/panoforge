# Antigravity Workspace Agent Instructions (background)

このプロジェクトの正確なルートパス:
`C:\Users\sx717\Antigravity\background`

## 基本ルール
1. 作業開始前に必ず `HANDOFF.md` と `docs/deploy.md` を読むこと。
2. 作業開始時に必ず `pwd` と `git status` を確認すること。
3. トークン限界・交代時は、必ず `HANDOFF.md` を更新してから引き継ぐこと。
4. このプロジェクトは 360度パノラマビューワーアプリです。ポート番号は 5175 を使用してください。
